# shim_npm_uses_package_manager_version

## `vp env exec node --version`

Ensure Node.js is installed first

```
<version>
```

## `npm --version`

npm shim resolves the pinned packageManager version (10.9.4)

```
10.9.4
```

## `npx --version`

npx shim resolves the pinned packageManager version (10.9.4)

```
10.9.4
```
